{
  "name": "ai-reader",
  "version": "1.0.0",
  "private": true,
  "description": "AI Reader - voice-controlled desktop assistant",
  "scripts": {
    "build:web": "cd electron-app && npm install && npm run react-build",
    "setup:python": "cd python-ai-service && python -m venv .venv && .venv\\Scripts\\python.exe -m pip install -r requirements.txt",
    "start:python": "cd python-ai-service && .venv\\Scripts\\python.exe -m app.main",
    "start:node": "cd node-backend && npm run dev",
    "start:electron": "cd electron-app && npm run dev",
    "start:local": "powershell -ExecutionPolicy Bypass -File scripts/start-all.ps1"
  }
}
